# Used by `npm run build` / production preview.
# Point this at your public backend URL (often an ngrok HTTPS URL).

VITE_API_BASE_URL=https://YOUR-BACKEND-SUBDOMAIN.ngrok-free.app
VITE_JITSI_DOMAIN=meet.uib-study.com
